This directory handles the acquisition of the scotland yard graph.

The current graph is in [`current/connections.json`](./current/connections.json). We still may have to manually verify the connections. But a certain part is already done.
